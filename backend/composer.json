{
  "name": "finance/backend",
  "description": "Finance App backend dependencies and test tooling configuration.",
  "type": "project",
  "require": {},
  "require-dev": {
    "phpunit/phpunit": "^11.5"
  },
  "scripts": {
    "test": "@php vendor/bin/phpunit --colors=always -c phpunit.xml"
  }
}

